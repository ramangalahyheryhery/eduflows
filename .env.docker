# Configuration Docker
NODE_ENV=production
APP_NAME=EduFlows

# Base de données PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=eduflows_db
DB_USER=eduflows_user
DB_PASSWORD=eduflows_production_password_123!

# Backend
BACKEND_PORT=3000
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Frontend
FRONTEND_PORT=8080

# URLs pour le frontend (pour information, non utilisées en Docker)
API_URL=http://localhost:3000
WEB_URL=http://localhost:8080
POSTGRES_HOST_PORT=5433  # NOUVEAU: Port sur votre machine